# Keep the image lean and secret-free: host venv, caches, env files and
# local output never belong in the backend image (COPY . . would grab them).
venv/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.env
*.log
output/
